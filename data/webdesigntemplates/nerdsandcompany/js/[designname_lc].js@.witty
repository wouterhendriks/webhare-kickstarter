[! Careful here, any subscript operators that should still be present in the
   final javascript file must be double escaped: [[ (but not ]])

!]"use strict";

require('../css/[designname_lc].scss');

// Load JS file(s)
var support = require('./support.js');

// get site config, containing info like dtapstage, imgroot, islive, as well as optional extra information from the backend
var siteconfig = support.getSiteConfig();
